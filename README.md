# We're so locked in.


we are using python 3.14, if everything explode try using it too

csv data files are too big to push remotely via git, convert the xlsx data yourself and add it to data/

How to run app: ```streamlit run home.py``` you need to be in same dir